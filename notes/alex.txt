sudo su -
su postgres
psql
drop DATABASE skillbox;
CREATE DATABASE skillbox;
CREATE USER codewizard WITH ENCRYPTED PASSWORD '1234567';
GRANT ALL PRIVILEGES ON DATABASE skillbox TO codewizard;
ALTER USER codewizard WITH SUPERUSER;



DROP TABLE IF EXISTS books;
DROP TABLE IF EXISTS authors;

CREATE TABLE books
(
    id          BIGSERIAL PRIMARY KEY,
    author      INT,
    author_name VARCHAR(250) NOT NULL,
    title       VARCHAR(250) NOT NULL,
    price_old   VARCHAR(250) DEFAULT NULL,
    price       VARCHAR(250) DEFAULT NULL
);

CREATE TABLE authors
(
    id     BIGSERIAL PRIMARY KEY,
    author VARCHAR(250) NOT NULL
);